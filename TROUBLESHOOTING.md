# TROUBLESHOOTING（常见问题与诊断）

> 双击「启动.bat」窗口一闪就没了？
> 按顺序：① 双击 **`诊断.bat`**（打印 Python 路径、各依赖版本、ffmpeg、Qt 能否建窗口）→
> ② 双击 **`调试启动.bat`**（保留控制台，报错完整显示）→
> ③ 看程序目录（或 `%TEMP%`）下的 **`启动日志.txt`**。
> 启动期出错还会弹系统对话框说明原因，不再"无声无息地消失"。
> 常见原因：没装依赖 / 杀软拦截；Store 版 Python 的 `pythonw.exe` 定位问题已由启动脚本自动处理。

**安装依赖时问"是否改用系统 Python 继续安装"？**
这是 `.venv` 创建失败（磁盘满、无权限等）或里面缺 pip 时的确认提示。
直接回车 = 取消，什么都不装（推荐：先排查原因再重跑）；只有输入 `y` 才会装进系统 Python。

**脚本提示 `.venv` 已损坏？**
一般是 `.venv` 文件夹被手动删了一半。删掉整个 `.venv` 文件夹，重跑一遍 `安装依赖.bat` 即可重建。
`诊断.bat` 的 `.venv` 专区会明确显示 `[OK]` / 缺失 / 已损坏三种状态。

**想彻底重建环境 / 换回系统 Python？**
删掉 `.venv` 文件夹就是"换回系统 Python"；再跑一遍 `安装依赖.bat` 就是"彻底重建"。
Linux/macOS 同理：`rm -rf .venv` 后重来一遍 README 里那三行命令。

**每次都要重新预览吗？**
只是首次调参时需要。参数定下来后，同一批素材「① 预览分段 → ② 开始分割」两步走；
输出相关参数（编码、CRF、模板、报告）改了不影响预览。

**提示"未找到 ffmpeg"？**
任选其一：①「设置 → 环境与依赖 → 一键安装 / 更新依赖」（装上 imageio-ffmpeg，自带 ffmpeg）；
② 下载 [ffmpeg 官方 Windows 版](https://www.gyan.dev/ffmpeg/builds/)，把 `bin` 目录加入 PATH；
③ 在设置页手动指定 `ffmpeg.exe` 路径。

**切片数量和想要的差很多？**
先调「检测阈值」（越小切得越多），再检查检测器的「内部最短场景(帧)」（默认 15，可改 0/1），
最后看「分段长度限制」是否合并或切分过度。

**不重编码时每段开头多几帧 / 少几帧？**
无损切割的正常现象（只能从关键帧开始切）。要帧级精确请选「重编码 H.264」。
想把各段拼回原片（恒定帧率素材；VFR 请先转成 CFR 再切）：视频必须「重编码 H.264」
（帧精确，这是前提）；音频能去则「去音轨」（零音频误差，拼回去与原片逐帧对应），
要留声则「转 AAC」（基本一致：段尾 AAC 帧量化约 21ms、每接缝约 21ms 间隙，
感知不到但非逐采样对应）；「复制」是每刀几十毫秒舍入，最不准。
不重编码时每段多带出来的帧会让拼回去的总时长变长——光转音频解决不了视频关键帧的问题。
极端要求音频也逐采样对应：在「附加 ffmpeg 参数」里加 `-c:a pcm_s16le`（容器选 MKV，
音频选项保持「转 AAC」），PCM 无帧量化、无解码延迟，拼回去逐采样对应（文件会大很多）。

**重编码时尾帧像下一段的首帧，会跳帧吗？**
重编码模式使用输入后准确寻址，并把目标帧数作为硬上限，对常见恒定帧率视频严格输出
`[起始帧, 结束帧)`，上一段不会包含结束帧。可变帧率（VFR）素材建议先转恒定帧率再切。

**长视频检测很慢？**
把「分析下采样倍率」设为 2~4，或用「跳帧间隔」（略降准确度）。速度主要看 CPU 与分辨率。

**CSV 用 Excel 打开乱码？**
不会——导出是带 BOM 的 UTF-8，可直接正常显示中文。

**界面文字变方块 / 乱码？**
Windows 一般自带微软雅黑；精简版系统请安装中文字体。

**打包后的 exe 被杀毒软件拦截？**
PyInstaller 程序容易被误报，属常见现象。把 exe 加入白名单即可（本项目用目录模式打包，已比单文件误报少）。

**处理时能中途停止吗？**
可以。点「停止」后检测在当前帧中断，正在切割的片段结束后停；已生成的文件保留。

**预览时内嵌播放器黑屏 / 报错播不了？**
个别编码（某些 HEVC 10bit、AV1）不受系统解码器支持。点播放器下方的「外部播放器试看」，
程序会把该段截成临时文件再用默认播放器打开，效果一样。连外部试看都失败请先确认源文件本身能播。

**参数调乱了怎么恢复？**
点「视频分割」页「场景检测」组右上角的「恢复默认参数」（二次确认，视频与输出目录不受影响）。
更彻底的「恢复出厂设置（含外观）」在「设置」页底部。

---

## 提 Issue 前请附上

1. Windows 版本、Python 版本（`诊断.bat` 的输出可直接贴）；
2. 界面标题栏里的程序版本号；
3. `启动日志.txt` 相关段落（启动失败时）；
4. 最小复现步骤（哪个视频、哪几个参数）。
