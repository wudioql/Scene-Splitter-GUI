# CHANGELOG

格式遵循 [Keep a Changelog](https://keepachangelog.com/)。早期版本日期未记录，以内容为准。

## [1.2.2] - 2026-09-12

### Added

- 安装依赖.bat：`.venv` 创建失败或缺 pip 时改为确认式回退（默认取消，不再静默装进系统 Python）；
  各启动脚本能识别已损坏的 `.venv`（有 `pyvenv.cfg` 但无 `python.exe`）并提示删了重建。
- Linux/macOS：验证代码无损运行（全回归通过），README 补充标准 `.venv` 流程与最小化系统补库提示。
- 预览试看：「只播本段」两侧新增「上一段 / 下一段」按钮（与表格联动，首/尾段自动禁用；在播时切段新段接着播）。
- 分段预览：播放器下方新增「删除本段 / 与上一段合并」操作行（看完即点，不用来回滚动）；新增 Delete 删除、M 合并快捷键（输入框内按键不受影响）。
- 分段预览：画面预览区可收起/展开（「收起画面」按钮，状态自动记住），收起后表格和播放器离得更近；收起时不抓图，省 CPU。
- 试看播放器：记住「循环/静音」勾选，下次启动保持。
- 首次使用：启动时自动在项目里建出默认配置（不用等第一次改设置）。

### Changed

- 配置位置：从家目录（`~/.scene_splitter_gui/config.json`）改为程序目录（便携，删文件夹即完全卸载）；首次启动自动把旧配置搬过来并清理旧文件。

### Fixed

- 切段卡顿：缩略图抓帧移入后台线程（单线程追最新 + 空闲预取邻段），连续切换不再阻塞界面；
  缩略图缓存加 60 段上限，避免长预览占用过多内存。
- 性能审计优化：日志框加 2000 行上限（默认详细日志下不再无界增长）；
  三处表格填充加 updates 守卫；批量添加去重 O(n²)→O(n)；
  任务线程对象用完即释放；外部试看线程加退出等待。
- 手动删/合后自动选中并载入相邻段（合并取合体段、删除取滑入段/新尾段），播放器不再空转致操作键全灰。
- 试看首帧：选中段未播时显示上段尾帧——毫秒换算改向上取整（修 NTSC 等非整数帧率的截断偏差）；暂停寻址后短暂点播刷新画面（静音、无感知）。
- 试看进度条：点按直接跳到点击处（之前点按无效、松手跳回，只有拖动管用）。
- 试看刷新点播：改为界面静默（不拨动进度条/时间/播放键），暂停态拖动只静寻址、松开后补一次刷新；切换分段不再出现"播一下又停"。
- 试看静音：之前 setMuted 在部分后端（Windows）无效、实际没有静音——改为以音量 0/恢复为准（附带好处：刷新点播在这些机器上也真正无声了）。
- 试看点播杂音：结束点播先恢复音量再暂停，暂停生效前的尾巴漏出一小声——改为停稳再恢复（收到已暂停或 300ms 兜底），tick 期间重复断言静音。
- 试看点播杂音（续）：前修仍挡不住“音量指令晚于播放生效”的头部竞态——改为暂停态常驻 0 音量（只在显式播放时给音量），点播 play 时后端早已 settling，从构造上无声；旧的延迟恢复机器同步删除。
- PyInstaller 打包：`assets/` 图标资源打进包（之前 EXE 会静默回退旧图标链）。
- `main.py` 增加 frozen 路径兼容（`sys._MEIPASS` / exe 同级目录）。

## [1.2.1]

### Fixed

- Windows 任务栏图标空白：改用自带场记板 `.ico`/`.png`（绝对路径，不依赖 QtSvg），
  并加 Win32 原生 `WM_SETICON` 兜底；大小图标分流（任务栏跟系统主题、标题栏跟 App 主题），
  App 内切换主题实时刷新。

### Removed

- 不再每次启动写 `启动日志.txt`（仅启动失败 / 运行期异常时写）。

## [1.2.0]

### Added

- 智能择优切分（默认开启，「视频分割」页可关）：检测时记录画面变化信号；
  合并过短片段用动态规划保留置信度和最高的切点，超长切分吸附到附近变化峰值；
  纯静态内容回退均匀切分；预览摘要与日志会标注是否生效。

## [1.1.1]

### Fixed

- 分段试看播放器高度异常。
- 「恢复默认参数」按钮移至「场景检测」组右上角（原页面底部）。

## [1.0.x]

早期版本：检测 / 预览 / 分割 / 批量 / 报告全流程可用；Fluent 界面与浅色/深色主题。
